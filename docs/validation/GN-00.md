# GN-00: preparação local do Git

Data: 2026-09-09. Ambiente: macOS, checkout principal do Git Notch.

## Resultado

Repositório inicializado nesta pasta. A `main` contém apenas os dois documentos originais no commit `8502f67`. A branch `chore/gn-00-repository-rules` adiciona regras, plano de 20 tickets, template de PR, ignores e este registro. A identidade Git existente foi reutilizada sem alteração global.

Estado: em avaliação. O remoto público `https://github.com/IcaroAguiar/gitnotch` foi criado por autorização do usuário. Main e branch foram enviadas. A PR será vinculada na entrega. GN-01A não foi iniciado.

## Verificação executada

Uma verificação Python chamou Git diretamente e confirmou a raiz do repositório, igualdade byte a byte dos dois documentos com `main`, hashes anteriores à inicialização, regras de ignore e os 20 tickets.

Resultado observado: todas essas verificações passaram.

O staging identificou uma regra de ignore externa ao projeto para `AGENTS.md`. A exceção local `!AGENTS.md` permite versionar as regras sem alterar a configuração global da máquina. A presença do arquivo no commit foi verificada após a correção.

Hashes SHA-256 preservados:

```text
3ba5eb127ea5c02e785044fe8afe84e6c21329287d05ce4f74d461950a674d02  git-notch-spec-v0.1.md
6d7d05f1b0578b77ae4cba76cab68c288f5b8c1e5ef93dbe7898ba3a630a972d  git-notch-backlog-v0.1.md
```

## Reproduza a avaliação

Execute na raiz do projeto:

```sh
git rev-parse --show-toplevel
git log --oneline --all
git diff --stat main...HEAD
git diff --check main...HEAD
git diff main -- git-notch-spec-v0.1.md git-notch-backlog-v0.1.md
shasum -a 256 git-notch-spec-v0.1.md git-notch-backlog-v0.1.md
git check-ignore .env .env.local node_modules/test dist/test target/test .DS_Store
git check-ignore pnpm-lock.yaml Cargo.lock .env.example
git status --short
git remote -v
```

A raiz deve ser esta pasta. O diff dos documentos originais deve estar vazio e os hashes devem coincidir com os valores acima. O primeiro check-ignore imprime todos os caminhos e retorna 0. O segundo não imprime caminhos e retorna 1, pois lockfiles e exemplo de ambiente podem ser versionados. O diff da branch contém apenas documentação e configuração do repositório. O status deve estar limpo depois do commit.

Este relatório acompanha o commit de preparação. O SHA final e a verificação pós-commit são informados na entrega para evitar uma autorreferência de hash neste arquivo.

## Limites

Não há aplicação, dependências novas, CI ou configuração de proteção de branch. A licença MIT foi adicionada a partir do template oficial da API do GitHub. Testes de UI, integração nativa e performance não se aplicam a esta alteração documental. Revisão e merge continuam pendentes. A PR será vinculada na entrega. Nenhum projeto observado foi alterado.
